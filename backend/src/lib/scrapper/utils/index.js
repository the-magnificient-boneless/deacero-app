const { postSendUrlsToApi } = require("./sendUrlsToApi");
const { getRandomUserAgent } = require("./randomUserAgent");
const { getNeedleFromTheHayStack } = require("./needleInTheHayStack");

module.exports = {
  postSendUrlsToApi,
  getRandomUserAgent,
  getNeedleFromTheHayStack,
};
